<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inventory Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #f8f9fa; }
        .container { margin-top: 50px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2>Daftar Inventaris Barang</h2>
            <span class="badge bg-primary">Role: Manager/Staff</span>
        </div>

        <div class="card shadow-sm">
            <div class="card-body">
                <table class="table table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th>Nama Barang</th>
                            <th>Kategori</th>
                            <th>Stok</th>
                            <th>Min. Stok</th>
                            <th>Lokasi Rak</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (items.length > 0) { %>
                            <% items.forEach(item => { %>
                                <tr class="<%= item.stock_qty <= item.min_stock ? 'table-danger' : '' %>">
                                    <td><strong><%= item.item_name %></strong></td>
                                    <td><%= item.category %></td>
                                    <td><%= item.stock_qty %></td>
                                    <td><%= item.min_stock %></td>
                                    <td><%= item.storage_location %></td>
                                </tr>
                            <% }) %>
                        <% } else { %>
                            <tr>
                                <td colspan="5" class="text-center text-muted">Belum ada data barang di database.</td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
        <p class="mt-3 text-muted small">* Baris merah menunjukkan stok di bawah batas minimum.</p>
    </div>
</body>
</html>